 <div class="w3-container">
          <h5 class="w3-opacity"><b><%= work.title %></b></h5>
          <h6 class="w3-text-teal"><i class="fa fa-calendar fa-fw w3-margin-right"></i><%= work.year %> <span class="w3-tag w3-teal w3-round"></span></h6>
          <p><%= work.description %></p>
          <hr>
        </div>
          
<p>
  <%= link_to 'Destroy Work', [work.resume, work],
               method: :delete,
               data: { confirm: 'Are you sure?' } %>
</p>